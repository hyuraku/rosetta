package unit

// Shared string constants used across the unit test suite. Factored out to
// satisfy the goconst linter and keep repeated literals in one place.
const (
	nodeID1   = "node1"
	nodeID2   = "node2"
	addrNode2 = "localhost:8081"

	valValue1    = "value1"
	valValue     = "value"
	valTestValue = "testvalue"
)
